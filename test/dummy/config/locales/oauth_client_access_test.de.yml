de:
  activerecord:
    models:
      oauth_client_access_test:
        one:  "Oauth client access test"
        other: "Oauth client access tests"
    attributes:
      oauth_client_access_test:
        test: "Test"
